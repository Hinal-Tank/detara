import React from 'react';

// WhatsApp is hidden by default — only shown as fallback when AI chat fails
// Triggered programmatically via window.openWhatsAppFallback()
export default function WhatsAppFloat() {
  return null;
}